package card;

/** The types of vehicles that can be seen on the cards. */
public enum CardType {
    /** Car. */
    CAR,
    /** Airplane. */
    AIRPLANE,
    /** Motorcycle. */
    MOTORCYCLE,
    /** Boat. */
    BOAT
}
